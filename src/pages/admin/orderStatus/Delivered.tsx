import AllOrderStatus from "@/components/admin/order-status/AllOrderStatus";

const Delivered = () => {
  return <AllOrderStatus querykey="all delivered" path="delivered" />;
};

export default Delivered;
