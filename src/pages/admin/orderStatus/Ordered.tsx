import AllOrderStatus from "@/components/admin/order-status/AllOrderStatus";

const Ordered = () => {
  return (
    <AllOrderStatus key="all ordered" querykey="all ordered" path="ordered" />
  );
};

export default Ordered;
